package org.nting.toolkit.ui.style.material;

import org.nting.data.PropertyId;
import org.nting.toolkit.component.ScrollPane;

public enum ScrollPanePropertyIds implements PropertyId<ScrollPane> {
    SCROLLBAR_COLOR, SLIDER_COLOR, SLIDER_HOVER_COLOR
}
